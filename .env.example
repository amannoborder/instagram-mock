# URL the dummy-IG fetches its live manifest (full account + posts) from.
# Local dev defaults to /manifest.json (served from public/).
# In production, point this at the company-hosted manifest the upload endpoint writes to.
VITE_MANIFEST_URL=/manifest.json
